unit umain;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, Forms, Controls, Graphics, Dialogs, StdCtrls, ExtCtrls;

type

  { TMain }

  TMain = class(TForm)
    bMail: TButton;
    bVK: TButton;
    bDiscord: TButton;
    LabelMain: TLabel;
  private

  public

  end;

var
  Main: TMain;

implementation

{$R *.lfm}

{ TMain }


end.

